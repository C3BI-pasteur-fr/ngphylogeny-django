from .base import *

DEBUG = True

ROOT_URLCONF = 'NGPhylogeny_fr.urls.local'

# INSTALLED_APPS += (
#     'debug_toolbar',
# )

# MIDDLEWARE += (
#     'debug_toolbar.middleware.DebugToolbarMiddleware',
# )

# DATABASES is set in base.py (env-var-based, sqlite fallback) - shared
# with prod.py.

